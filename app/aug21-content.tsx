import fs from 'node:fs';import path from 'node:path';import type {Metadata} from 'next';import {Header,Footer,CTA} from './components';import meta from './aug21-meta.json';
const SITE='https://overseasvirtualassistant.com';type Item={family:'blog';title:string;description:string;hero:string};
const items=meta as Record<string,Item>;
function source(slug:string){return fs.readFileSync(path.join(process.cwd(),'content','blog',`aug21-${slug}.md`),'utf8').replace(/^---[\s\S]*?---\s*/,'')}
function blocks(text:string){return text.split(/\n\n+/).filter(Boolean).map((x,i)=>x.startsWith('## ')?<h2 key={i}>{x.slice(3)}</h2>:x.startsWith('- ')?<ul key={i}>{x.split('\n').map((y,j)=><li key={j}>{y.replace(/^- /,'')}</li>)}</ul>:<p key={i}>{x}</p>)}
export function getAug21Metadata(slug:string):Metadata{const x=items[slug];return {title:x.title,description:x.description,alternates:{canonical:`/blog/${slug}`},openGraph:{title:x.title,description:x.description,url:`${SITE}/blog/${slug}`,images:[x.hero]}}}
export function renderAug21Article(slug:string){const x=items[slug];const schema={'@context':'https://schema.org','@type':'Article',headline:x.title,datePublished:'2026-08-21',dateModified:'2026-08-21',mainEntityOfPage:`${SITE}/blog/${slug}`,image:`${SITE}${x.hero}`};return <><Header/><main className="article-shell"><article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><p className="eyebrow">Blog</p><h1>{x.title}</h1><p>{x.description}</p><p><time dateTime="2026-08-21">August 21, 2026</time></p><img src={x.hero} alt={`${x.title} editorial illustration`} width="1536" height="1024" style={{width:'100%',height:'auto',borderRadius:'18px'}}/><div className="article-body">{blocks(source(slug))}</div></article><CTA/></main><Footer/></>}
